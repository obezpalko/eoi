# Upstream Quartz defaults
public
node_modules
.quartz-cache

# Obsidian vault config and vendored community-plugin bundles (minified
# third-party main.js/styles.css — not ours to format).
.obsidian

# Vault content. Notes are authored in Obsidian and normalised by the
# obsidian-linter plugin; letting prettier also rewrite them would fight
# that plugin over list markers, emphasis characters and line wrapping.
/[0-9][0-9]-*/
/*.md
scratch

# Google Search Console verification token — must be served byte-for-byte.
quartz/root/googled40ea25e54b9d2d0.html
